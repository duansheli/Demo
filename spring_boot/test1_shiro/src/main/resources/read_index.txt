
tag:A.2 
1. 用户登陆，退出
2. 密码采用md5加密 加盐


docker run \
  -v /myredis/conf/redis.conf:/usr/local/etc/redis/redis.conf \
  --name myredis redis \
  redis-server /usr/local/etc/redis/redis.conf


有项目合同： 工资可以要高点，不交金
交金按最低交，还是

-----------------------------------------------------

shiro jsessionid  sessionManager
记住我功能
-------------------------------------------------------
tomcat session定制 ？？ 通过定制关闭session功能， 或者使用其他http服务，比如undertow
